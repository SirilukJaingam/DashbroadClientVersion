import LiteColorMaps_default from "./LiteColorMaps.json.js";
//#region Sources/Rendering/Core/ColorTransferFunction/ColorMapsLite.js
var presetMap = Object.create(null);
LiteColorMaps_default.filter((p) => p.RGBPoints).filter((p) => p.ColorSpace !== "CIELAB").forEach((p) => {
	presetMap[p.Name] = p;
});
var rgbPresetNames = Object.keys(presetMap);
rgbPresetNames.sort();
function getPresetByName(name) {
	return presetMap[name];
}
function addPreset(preset) {
	if (!preset.RGBPoints || preset.ColorSpace === "CIELAB") return;
	if (!presetMap[preset.Name]) {
		rgbPresetNames.push(preset.Name);
		rgbPresetNames.sort();
	}
	presetMap[preset.Name] = preset;
}
function removePresetByName(name) {
	const index = rgbPresetNames.indexOf(name);
	if (index > -1) rgbPresetNames.splice(index, 1);
	delete presetMap[name];
}
var ColorMapsLite_default = {
	addPreset,
	removePresetByName,
	getPresetByName,
	rgbPresetNames
};
//#endregion
export { ColorMapsLite_default as default };

//# sourceMappingURL=ColorMapsLite.js.map