import macros_default from "../../macros.js";
//#region Sources/Common/DataModel/Molecule/index.js
var MOLECULE_FIELDS = [
	"chemical json",
	"name",
	"inchi",
	"formula",
	"atoms",
	"bonds",
	"properties"
];
function vtkMolecule(publicAPI, model) {
	model.classHierarchy.push("vtkMolecule");
}
var DEFAULT_VALUES = {
	"chemical json": 0,
	name: "",
	inchi: "",
	formula: "",
	atoms: null,
	bonds: null,
	properties: null
};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	if (!model.atoms) model.atoms = {};
	if (!model.bonds) model.bonds = {};
	if (!model.properties) model.properties = {};
	macros_default.setGet(publicAPI, model, MOLECULE_FIELDS);
	vtkMolecule(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkMolecule");
var Molecule_default = {
	newInstance,
	extend
};
//#endregion
export { Molecule_default as default, extend, newInstance };

//# sourceMappingURL=Molecule.js.map