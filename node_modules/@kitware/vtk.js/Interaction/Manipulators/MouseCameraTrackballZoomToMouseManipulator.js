import macros_default from "../../macros.js";
import InteractorStyleManipulator_default from "../Style/InteractorStyleManipulator.js";
import MouseCameraTrackballZoomManipulator_default from "./MouseCameraTrackballZoomManipulator.js";
//#region Sources/Interaction/Manipulators/MouseCameraTrackballZoomToMouseManipulator/index.js
function vtkMouseCameraTrackballZoomToMouseManipulator(publicAPI, model) {
	model.classHierarchy.push("vtkMouseCameraTrackballZoomToMouseManipulator");
	const superOnButtonDown = publicAPI.onButtonDown;
	publicAPI.onButtonDown = (interactor, renderer, position) => {
		superOnButtonDown(interactor, renderer, position);
		model.zoomPosition = position;
	};
	publicAPI.onMouseMove = (interactor, renderer, position) => {
		if (!position) return;
		const k = (model.previousPosition.y - position.y) * model.zoomScale;
		InteractorStyleManipulator_default.dollyToPosition(1 - k, model.zoomPosition, renderer, interactor);
		if (interactor.getLightFollowCamera()) renderer.updateLightsGeometryToFollowCamera();
		model.previousPosition = position;
	};
	publicAPI.onScroll = (interactor, renderer, delta, position) => {
		if (!delta || !position) return;
		const dyf = 1 - delta * .1;
		InteractorStyleManipulator_default.dollyToPosition(dyf, position, renderer, interactor);
		if (interactor.getLightFollowCamera()) renderer.updateLightsGeometryToFollowCamera();
	};
}
var DEFAULT_VALUES = { zoomPosition: null };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	MouseCameraTrackballZoomManipulator_default.extend(publicAPI, model, initialValues);
	vtkMouseCameraTrackballZoomToMouseManipulator(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkMouseCameraTrackballZoomToMouseManipulator");
var MouseCameraTrackballZoomToMouseManipulator_default = {
	newInstance,
	extend
};
//#endregion
export { MouseCameraTrackballZoomToMouseManipulator_default as default, extend, newInstance };

//# sourceMappingURL=MouseCameraTrackballZoomToMouseManipulator.js.map