import macros_default from "../../macros.js";
import PolyData_default from "../../Common/DataModel/PolyData.js";
import Actor_default from "../../Rendering/Core/Actor.js";
import Mapper_default from "../../Rendering/Core/Mapper.js";
import { allocateArray } from "./WidgetRepresentation.js";
import ContextRepresentation_default from "./ContextRepresentation.js";
import { origin } from "./GlyphRepresentation.js";
//#region Sources/Widgets/Representations/CroppingOutlineRepresentation/index.js
var { vtkErrorMacro } = macros_default;
var OUTLINE_ARRAY = [
	2,
	0,
	1,
	2,
	0,
	2,
	2,
	0,
	4,
	2,
	1,
	3,
	2,
	1,
	5,
	2,
	2,
	3,
	2,
	2,
	6,
	2,
	3,
	7,
	2,
	4,
	5,
	2,
	4,
	6,
	2,
	5,
	7,
	2,
	6,
	7
];
function vtkCroppingOutlineRepresentation(publicAPI, model) {
	model.classHierarchy.push("vtkCroppingOutlineRepresentation");
	model.internalPolyData = PolyData_default.newInstance({ mtime: 0 });
	allocateArray(model.internalPolyData, "lines", OUTLINE_ARRAY.length).getData().set(OUTLINE_ARRAY);
	const applyOrigin = origin(publicAPI, model);
	model.mapper = Mapper_default.newInstance({ scalarVisibility: false });
	model.actor = Actor_default.newInstance({ parentProp: publicAPI });
	model.actor.getProperty().setEdgeColor(...model.edgeColor);
	model.mapper.setInputConnection(publicAPI.getOutputPort());
	model.actor.setMapper(model.mapper);
	publicAPI.addActor(model.actor);
	publicAPI.requestData = (inData, outData) => {
		const list = publicAPI.getRepresentationStates(inData[0]).filter((state) => state.getOrigin && state.getOrigin());
		if (list.length === 8) {
			applyOrigin(model.internalPolyData, list);
			model.internalPolyData.getPoints().modified();
			model.internalPolyData.modified();
			outData[0] = model.internalPolyData;
		} else vtkErrorMacro("CroppingOutlineRepresentation did not get 8 states");
	};
}
var DEFAULT_VALUES = { edgeColor: [
	1,
	1,
	1
] };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	ContextRepresentation_default.extend(publicAPI, model, initialValues);
	macros_default.setGetArray(publicAPI, model, ["edgeColor"], 3);
	macros_default.get(publicAPI, model, ["mapper", "actor"]);
	vtkCroppingOutlineRepresentation(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkCroppingOutlineRepresentation");
var CroppingOutlineRepresentation_default = {
	newInstance,
	extend
};
//#endregion
export { CroppingOutlineRepresentation_default as default, extend, newInstance };

//# sourceMappingURL=CroppingOutlineRepresentation.js.map