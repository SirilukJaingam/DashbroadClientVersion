import macros_default from "../../macros.js";
import PolyData_default from "../../Common/DataModel/PolyData.js";
//#region Sources/Filters/General/LineFilter/index.js
function vtkLineFilter(publicAPI, model) {
	model.classHierarchy.push("vtkLineFilter");
	publicAPI.requestData = (inData, outData) => {
		const dataset = outData[0]?.initialize() || PolyData_default.newInstance();
		dataset.getPoints().setData(inData[0].getPoints().getData());
		dataset.getLines().setData(inData[0].getLines().getData());
		outData[0] = dataset;
	};
}
var DEFAULT_VALUES = {};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.setGet(publicAPI, model, []);
	macros_default.obj(publicAPI, model);
	macros_default.algo(publicAPI, model, 1, 1);
	vtkLineFilter(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkLineFilter");
var LineFilter_default = {
	newInstance,
	extend
};
//#endregion
export { LineFilter_default as default, extend, newInstance };

//# sourceMappingURL=LineFilter.js.map