//#region Sources/Common/Core/Endian/index.js
function getEndianness() {
	const a = /* @__PURE__ */ new ArrayBuffer(4);
	const b = new Uint8Array(a);
	const c = new Uint32Array(a);
	b[0] = 161;
	b[1] = 178;
	b[2] = 195;
	b[3] = 212;
	if (c[0] === 3569595041) return "LittleEndian";
	if (c[0] === 2712847316) return "BigEndian";
	return null;
}
var ENDIANNESS = getEndianness();
function swapBytes(buffer, wordSize) {
	if (wordSize < 2) return;
	const bytes = new Int8Array(buffer);
	const size = bytes.length;
	const tempBuffer = [];
	for (let i = 0; i < size; i += wordSize) {
		for (let j = 0; j < wordSize; j++) tempBuffer.push(bytes[i + j]);
		for (let j = 0; j < wordSize; j++) bytes[i + j] = tempBuffer.pop();
	}
}
var Endian_default = {
	ENDIANNESS,
	getEndianness,
	swapBytes
};
//#endregion
export { ENDIANNESS, Endian_default as default, getEndianness, swapBytes };

//# sourceMappingURL=Endian.js.map