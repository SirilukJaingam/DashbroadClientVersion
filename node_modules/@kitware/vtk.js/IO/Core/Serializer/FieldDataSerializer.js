import DataSetAttributes_default from "../../../Common/DataModel/DataSetAttributes.js";
//#region Sources/IO/Core/Serializer/FieldDataSerializer.js
var CLASS_NAME = "vtkDataSetAttributes";
var ARRAYS = [
	"Scalars",
	"Vectors",
	"Normals",
	"TCoords",
	"Tensors",
	"GlobalIds",
	"PedigreeIds"
];
function canSerialize(obj) {
	return obj && obj.isA && obj.isA(CLASS_NAME);
}
function canDeserialize(obj) {
	return obj && obj.vtkClass && obj.vtkClass === CLASS_NAME;
}
function serialize(obj, arrayHandler) {
	const output = { vtkClass: CLASS_NAME };
	const indexMapping = [];
	const arrays = obj.getArrays();
	for (let i = 0; i < arrays.length; i++) indexMapping.push(arrayHandler.serialize(arrays[i]));
	ARRAYS.forEach((attrType) => {
		const arrayIdx = obj[`getActive${attrType}`]();
		if (arrayIdx !== -1) output[attrType] = indexMapping[arrayIdx];
	});
	output.arrays = indexMapping;
	return output;
}
function deserialize(obj, arrayHandler) {
	const ds = DataSetAttributes_default.newInstance();
	for (let i = 0; i < obj.arrays.length; i++) ds.addArray(arrayHandler.deserialize(obj.arrays[i]));
	ARRAYS.forEach((attrType) => {
		ds[`set${attrType}`](arrayHandler.deserialize(obj[attrType]));
	});
	return ds;
}
var FieldDataSerializer_default = {
	canSerialize,
	serialize,
	canDeserialize,
	deserialize
};
//#endregion
export { FieldDataSerializer_default as default };

//# sourceMappingURL=FieldDataSerializer.js.map