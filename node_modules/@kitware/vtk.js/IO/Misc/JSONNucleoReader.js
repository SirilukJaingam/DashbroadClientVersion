import macros_default from "../../macros.js";
import PolyData_default from "../../Common/DataModel/PolyData.js";
import DataAccessHelper_default from "../Core/DataAccessHelper.js";
import "../Core/DataAccessHelper/LiteHttpDataAccessHelper.js";
//#region Sources/IO/Misc/JSONNucleoReader/index.js
function vtkJSONNucleoReader(publicAPI, model) {
	model.classHierarchy.push("vtkJSONNucleoReader");
	if (!model.dataAccessHelper) model.dataAccessHelper = DataAccessHelper_default.get("http");
	function fetchText(url, options) {
		return model.dataAccessHelper.fetchText(publicAPI, url, options);
	}
	publicAPI.setUrl = (url, options) => {
		model.url = url;
		return publicAPI.loadData(options);
	};
	publicAPI.loadData = (options) => fetchText(model.url, options).then((csv) => {
		publicAPI.parseAsText(csv);
		return true;
	});
	publicAPI.parseAsText = (jsonAsTxt) => {
		const { vertices, indices } = JSON.parse(jsonAsTxt);
		const nbIndices = indices.length;
		const nbCellsValues = nbIndices / 3 + nbIndices;
		model.points = Float32Array.from(vertices);
		model.polys = nbCellsValues < 65535 ? new Uint16Array(nbCellsValues) : new Uint32Array(nbCellsValues);
		let srcOffset = 0;
		let destOffset = 0;
		while (destOffset < model.polys.length) {
			model.polys[destOffset++] = 3;
			model.polys[destOffset++] = indices[srcOffset++];
			model.polys[destOffset++] = indices[srcOffset++];
			model.polys[destOffset++] = indices[srcOffset++];
		}
		publicAPI.modified();
	};
	publicAPI.requestData = (inData, outData) => {
		const polydata = PolyData_default.newInstance();
		polydata.getPoints().setData(model.points, 3);
		polydata.getPolys().setData(model.polys);
		model.output[0] = polydata;
	};
}
var DEFAULT_VALUES = {};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	macros_default.get(publicAPI, model, ["url"]);
	macros_default.setGet(publicAPI, model, ["dataAccessHelper"]);
	macros_default.algo(publicAPI, model, 0, 1);
	vtkJSONNucleoReader(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkJSONNucleoReader");
var JSONNucleoReader_default = {
	newInstance,
	extend
};
//#endregion
export { JSONNucleoReader_default as default, extend, newInstance };

//# sourceMappingURL=JSONNucleoReader.js.map