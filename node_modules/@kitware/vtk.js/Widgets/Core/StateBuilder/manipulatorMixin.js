import macros_default from "../../../macros.js";
//#region Sources/Widgets/Core/StateBuilder/manipulatorMixin.js
function vtkManipulatorMixin(publicAPI, model) {
	publicAPI.updateManipulator = () => {
		if (model.manipulator) {
			const { origin, normal, direction } = model;
			const { setHandleOrigin, setHandleCenter, setHandleNormal, setHandleDirection } = model.manipulator;
			if (origin && setHandleOrigin) setHandleOrigin(origin);
			else if (origin && setHandleCenter) setHandleCenter(origin);
			if (direction && setHandleDirection) setHandleDirection(direction);
			else if (direction && !normal && setHandleNormal) setHandleNormal(direction);
			else if (normal && setHandleDirection) setHandleDirection(normal);
		}
	};
}
var DEFAULT_VALUES = { manipulator: null };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.setGet(publicAPI, model, ["manipulator"]);
	vtkManipulatorMixin(publicAPI, model);
}
var manipulatorMixin_default = { extend };
//#endregion
export { manipulatorMixin_default as default, extend };

//# sourceMappingURL=manipulatorMixin.js.map