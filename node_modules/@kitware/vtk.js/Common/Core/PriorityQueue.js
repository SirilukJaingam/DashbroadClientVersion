import macros_default from "../../macros.js";
//#region Sources/Common/Core/PriorityQueue/index.js
function vtkPriorityQueue(publicAPI, model) {
	model.classHierarchy.push("vtkPriorityQueue");
	publicAPI.push = (priority, element) => {
		const i = model.elements.findIndex((e) => e.priority > priority);
		model.elements.splice(i, 0, {
			priority,
			element
		});
	};
	publicAPI.pop = () => {
		if (model.elements.length > 0) return model.elements.shift().element;
		return null;
	};
	publicAPI.deleteById = (id) => {
		model.elements = model.elements.filter(({ element }) => element.id !== id);
	};
	publicAPI.length = () => model.elements.length;
}
var DEFAULT_VALUES = { elements: [] };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	vtkPriorityQueue(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkPriorityQueue");
var PriorityQueue_default = {
	newInstance,
	extend
};
//#endregion
export { PriorityQueue_default as default, extend, newInstance };

//# sourceMappingURL=PriorityQueue.js.map