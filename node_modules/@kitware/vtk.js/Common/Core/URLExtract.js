//#region Sources/Common/Core/URLExtract/index.js
function identity(i) {
	return i;
}
function toNativeType(str) {
	if (str === null || str === "null") return null;
	if (str === "true") return true;
	if (str === "false") return false;
	if (str === void 0 || str === "undefined") return;
	if (str[0] === "[" && str[str.length - 1] === "]") return str.substring(1, str.length - 1).split(",").map((s) => toNativeType(s.trim()));
	if (str === "" || Number.isNaN(Number(str))) return str;
	return Number(str);
}
function extractURLParameters(castToNativeType = true, query = window.location.search) {
	const summary = {};
	const convert = castToNativeType ? toNativeType : identity;
	new URLSearchParams(query).forEach((value, key) => {
		if (key) summary[key] = value ? convert(value) : true;
	});
	return summary;
}
var URLExtract_default = {
	toNativeType,
	extractURLParameters
};
//#endregion
export { URLExtract_default as default };

//# sourceMappingURL=URLExtract.js.map