import macros_default from "../../../macros.js";
import { vec3 } from "gl-matrix";
//#region Sources/Widgets/Widgets3D/PaintWidget/behavior.js
function widgetBehavior(publicAPI, model) {
	model.painting = model._factory.getPainting();
	publicAPI.handleLeftButtonPress = (callData) => {
		const manipulator = model.activeState?.getManipulator?.() ?? model.manipulator;
		if (!(manipulator && model.activeState && model.activeState.getActive())) {
			model.painting = false;
			return macros_default.VOID;
		}
		const { worldCoords } = manipulator.handleEvent(callData, model._apiSpecificRenderWindow);
		if (!worldCoords?.length) {
			model.painting = false;
			return macros_default.VOID;
		}
		model.painting = true;
		model.widgetState.addTrail().set(model.activeState.get("origin", "up", "right", "direction", "scale1"));
		publicAPI.invokeStartInteractionEvent();
		return macros_default.EVENT_ABORT;
	};
	publicAPI.handleMouseMove = (callData) => publicAPI.handleEvent(callData);
	publicAPI.handleLeftButtonRelease = () => {
		if (model.painting) {
			publicAPI.invokeEndInteractionEvent();
			model.widgetState.clearTrailList();
		}
		model.painting = false;
		return macros_default.VOID;
	};
	publicAPI.handleEvent = (callData) => {
		const manipulator = model.activeState?.getManipulator?.() ?? model.manipulator;
		if (!(manipulator && model.activeState && model.activeState.getActive())) {
			model.painting = false;
			return macros_default.VOID;
		}
		const normal = model._camera.getDirectionOfProjection();
		const up = model._camera.getViewUp();
		const right = [];
		vec3.cross(right, up, normal);
		model.activeState.setUp(...up);
		model.activeState.setRight(...right);
		model.activeState.setDirection(...normal);
		const { worldCoords } = manipulator.handleEvent(callData, model._apiSpecificRenderWindow);
		if (!worldCoords?.length) return macros_default.VOID;
		model.widgetState.setTrueOrigin(...worldCoords);
		model.activeState.setOrigin(...worldCoords);
		if (model.painting) model.widgetState.addTrail().set(model.activeState.get("origin", "up", "right", "direction", "scale1"));
		else return macros_default.VOID;
		publicAPI.invokeInteractionEvent();
		return macros_default.EVENT_ABORT;
	};
	publicAPI.grabFocus = () => {
		if (!model.hasFocus) {
			model.activeState = model.widgetState.getHandle();
			model.activeState.activate();
			model._interactor.requestAnimation(publicAPI);
			const canvas = model._apiSpecificRenderWindow.getCanvas();
			canvas.onmouseenter = () => {
				if (model.hasFocus && model.activeState === model.widgetState.getHandle()) model.activeState.setVisible(true);
			};
			canvas.onmouseleave = () => {
				if (model.hasFocus && model.activeState === model.widgetState.getHandle()) model.activeState.setVisible(false);
			};
		}
		model.hasFocus = true;
	};
	publicAPI.loseFocus = () => {
		if (model.hasFocus) model._interactor.cancelAnimation(publicAPI);
		model.widgetState.deactivate();
		model.widgetState.getHandle().deactivate();
		model.activeState = null;
		model.hasFocus = false;
	};
	macros_default.get(publicAPI, model, ["painting"]);
}
//#endregion
export { widgetBehavior as default };

//# sourceMappingURL=behavior.js.map