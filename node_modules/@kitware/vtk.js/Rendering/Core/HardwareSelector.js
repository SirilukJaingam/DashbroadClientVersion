import macros_default from "../../macros.js";
import DataSet_default from "../../Common/DataModel/DataSet.js";
//#region Sources/Rendering/Core/HardwareSelector/index.js
var { FieldAssociations } = DataSet_default;
function vtkHardwareSelector(publicAPI, model) {
	model.classHierarchy.push("vtkHardwareSelector");
	publicAPI.getSourceDataAsync = async (renderer, fx1, fy1, fx2, fy2) => {};
	publicAPI.selectAsync = async (renderer, fx1, fy1, fx2, fy2) => {
		const srcData = await publicAPI.getSourceDataAsync(renderer, fx1, fy1, fx2, fy2);
		if (srcData) return srcData.generateSelection(fx1, fy1, fx2, fy2);
		return [];
	};
}
var DEFAULT_VALUES = {
	fieldAssociation: FieldAssociations.FIELD_ASSOCIATION_CELLS,
	captureZValues: false
};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	macros_default.setGet(publicAPI, model, ["fieldAssociation", "captureZValues"]);
	vtkHardwareSelector(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkHardwareSelector");
var HardwareSelector_default = {
	newInstance,
	extend
};
//#endregion
export { HardwareSelector_default as default, extend, newInstance };

//# sourceMappingURL=HardwareSelector.js.map