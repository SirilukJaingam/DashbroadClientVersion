import macros_default from "../../macros.js";
import Plane_default from "../../Common/DataModel/Plane.js";
import AbstractManipulator_default from "./AbstractManipulator.js";
//#region Sources/Widgets/Manipulators/PlaneManipulator/index.js
function intersectDisplayWithPlane(x, y, planeOrigin, planeNormal, renderer, glRenderWindow) {
	const near = glRenderWindow.displayToWorld(x, y, 0, renderer);
	const far = glRenderWindow.displayToWorld(x, y, 1, renderer);
	return Plane_default.intersectWithLine(near, far, planeOrigin, planeNormal).x;
}
function vtkPlaneManipulator(publicAPI, model) {
	model.classHierarchy.push("vtkPlaneManipulator");
	publicAPI.handleEvent = (callData, glRenderWindow) => model._addWorldDeltas({ worldCoords: intersectDisplayWithPlane(callData.position.x, callData.position.y, publicAPI.getOrigin(callData), publicAPI.getNormal(callData), callData.pokedRenderer, glRenderWindow) });
}
function defaultValues(initialValues) {
	return { ...initialValues };
}
function extend(publicAPI, model, initialValues = {}) {
	AbstractManipulator_default.extend(publicAPI, model, defaultValues(initialValues));
	vtkPlaneManipulator(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkPlaneManipulator");
var PlaneManipulator_default = {
	intersectDisplayWithPlane,
	extend,
	newInstance
};
//#endregion
export { PlaneManipulator_default as default, extend, intersectDisplayWithPlane, newInstance };

//# sourceMappingURL=PlaneManipulator.js.map