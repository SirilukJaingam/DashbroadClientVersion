//#region Sources/Common/Core/HalfFloat/index.js
var floatView = new Float32Array(1);
var int32View = new Int32Array(floatView.buffer);
function toHalf(val) {
	floatView[0] = val;
	const x = int32View[0];
	let bits = x >> 16 & 32768;
	let m = x >> 12 & 2047;
	const e = x >> 23 & 255;
	if (e < 103) return bits;
	if (e > 142) {
		bits |= 31744;
		bits |= (e === 255 ? 0 : 1) && x & 8388607;
		return bits;
	}
	if (e < 113) {
		m |= 2048;
		bits |= (m >> 114 - e) + (m >> 113 - e & 1);
		return bits;
	}
	bits |= e - 112 << 10 | m >> 1;
	bits += m & 1;
	return bits;
}
function fromHalf(h) {
	const s = (h & 32768) >> 15;
	const e = (h & 31744) >> 10;
	const f = h & 1023;
	if (e === 0) return (s ? -1 : 1) * 2 ** -14 * (f / 2 ** 10);
	if (e === 31) return f ? NaN : (s ? -1 : 1) * Infinity;
	return (s ? -1 : 1) * 2 ** (e - 15) * (1 + f / 2 ** 10);
}
var HalfFloat_default = {
	fromHalf,
	toHalf
};
//#endregion
export { HalfFloat_default as default };

//# sourceMappingURL=HalfFloat.js.map