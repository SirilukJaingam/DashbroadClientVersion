import macros_default from "../../macros.js";
//#region Sources/IO/Misc/JSONReader/index.js
function vtkJSONReader(publicAPI, model) {
	model.classHierarchy.push("vtkJSONReader");
	function fetchData(url, option = {}) {
		return model.dataAccessHelper.fetchText(publicAPI, url, option);
	}
	publicAPI.setUrl = (url, option = {}) => {
		model.url = url;
		return publicAPI.loadData(option);
	};
	publicAPI.loadData = (option = {}) => fetchData(model.url, option).then(publicAPI.parseAsText);
	publicAPI.parseAsText = (content) => {
		if (!content) return false;
		model.data = JSON.parse(content);
		publicAPI.modified();
		return true;
	};
	publicAPI.requestData = (inData, outData) => {
		outData[0] = model.data;
	};
	publicAPI.isBusy = () => false;
	publicAPI.getNumberOfOutputPorts = () => model.numberOfOutputs;
}
var DEFAULT_VALUES = {};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	macros_default.get(publicAPI, model, ["url"]);
	macros_default.algo(publicAPI, model, 0, 1);
	macros_default.event(publicAPI, model, "busy");
	vtkJSONReader(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkJSONReader");
var JSONReader_default = {
	newInstance,
	extend
};
//#endregion
export { JSONReader_default as default, extend, newInstance };

//# sourceMappingURL=JSONReader.js.map