import macros_default from "../../macros.js";
import Prop3D_default from "./Prop3D.js";
import VolumeProperty_default from "./VolumeProperty.js";
//#region Sources/Rendering/Core/Volume/index.js
function vtkVolume(publicAPI, model) {
	model.classHierarchy.push("vtkVolume");
	publicAPI.getVolumes = () => [publicAPI];
	publicAPI.makeProperty = VolumeProperty_default.newInstance;
	publicAPI.getRedrawMTime = () => {
		let mt = model.mtime;
		if (model.mapper !== null) {
			let time = model.mapper.getMTime();
			mt = time > mt ? time : mt;
			if (model.mapper.getInput() !== null) {
				model.mapper.getInputAlgorithm().update();
				time = model.mapper.getInput().getMTime();
				mt = time > mt ? time : mt;
			}
		}
		return mt;
	};
}
var DEFAULT_VALUES = { mapper: null };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	Prop3D_default.extend(publicAPI, model, initialValues);
	model.boundsMTime = {};
	macros_default.obj(model.boundsMTime);
	macros_default.setGet(publicAPI, model, ["mapper"]);
	vtkVolume(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkVolume");
var Volume_default = {
	newInstance,
	extend
};
//#endregion
export { Volume_default as default, extend, newInstance };

//# sourceMappingURL=Volume.js.map