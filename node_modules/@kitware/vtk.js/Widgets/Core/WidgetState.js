import macros_default from "../../macros.js";
//#region Sources/Widgets/Core/WidgetState/index.js
var DEFAULT_LABEL = "default";
function removeObjectInArray(array, obj) {
	const idx = array.indexOf(obj);
	if (idx !== -1) array.splice(idx, 1);
}
function vtkWidgetState(publicAPI, model) {
	model.classHierarchy.push("vtkWidgetState");
	const subscriptions = [];
	model.labels = {};
	model.nestedStates = [];
	publicAPI.bindState = (nested, labels = [DEFAULT_LABEL]) => {
		model.nestedStates.push(nested);
		subscriptions.push(nested.onModified(publicAPI.modified));
		if (Array.isArray(labels) && labels.length) for (let i = 0; i < labels.length; i++) {
			const label = labels[i];
			if (!model.labels[label]) model.labels[label] = [];
			model.labels[label].push(nested);
		}
		else {
			const labelToUse = Array.isArray(labels) ? DEFAULT_LABEL : labels || DEFAULT_LABEL;
			if (!model.labels[labelToUse]) model.labels[labelToUse] = [];
			model.labels[labelToUse].push(nested);
		}
	};
	publicAPI.unbindState = (nested) => {
		while (subscriptions.length) subscriptions.pop().unsubscribe();
		removeObjectInArray(model.nestedStates, nested);
		for (let i = 0; i < model.nestedStates.length; i++) subscriptions.push(model.nestedStates[i].onModified(publicAPI.modified));
		Object.keys(model.labels).forEach((label) => {
			const list = model.labels[label];
			removeObjectInArray(list, nested);
		});
	};
	publicAPI.unbindAll = () => {
		while (subscriptions.length) subscriptions.pop().unsubscribe();
		model.nestedStates = [];
	};
	publicAPI.activate = () => publicAPI.setActive(true);
	publicAPI.deactivate = (excludingState) => {
		if (excludingState !== publicAPI) publicAPI.setActive(false);
		for (let i = 0; i < model.nestedStates.length; i++) model.nestedStates[i].deactivate(excludingState);
	};
	publicAPI.activateOnly = (subState) => {
		if (subState) subState.setActive(true);
		publicAPI.deactivate(subState);
	};
	publicAPI.getStatesWithLabel = (name) => model.labels[name];
	publicAPI.getAllNestedStates = () => model.nestedStates;
	publicAPI.delete = macros_default.chain(publicAPI.unbindAll, publicAPI.delete);
}
var DEFAULT_VALUES = { active: false };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	macros_default.setGet(publicAPI, model, ["active"]);
	vtkWidgetState(publicAPI, model);
}
var WidgetState_default = { extend };
//#endregion
export { WidgetState_default as default, extend };

//# sourceMappingURL=WidgetState.js.map