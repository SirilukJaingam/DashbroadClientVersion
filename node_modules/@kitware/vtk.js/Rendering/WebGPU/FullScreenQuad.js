import macros_default from "../../macros.js";
import ShaderCache_default from "./ShaderCache.js";
import SimpleMapper_default from "./SimpleMapper.js";
//#region Sources/Rendering/WebGPU/FullScreenQuad/index.js
function vtkWebGPUFullScreenQuad(publicAPI, model) {
	model.classHierarchy.push("vtkWebGPUFullScreenQuad");
	publicAPI.replaceShaderPosition = (hash, pipeline, vertexInput) => {
		const vDesc = pipeline.getShaderDescription("vertex");
		vDesc.addBuiltinOutput("vec4<f32>", "@builtin(position) Position");
		vDesc.addOutput("vec4<f32>", "vertexVC");
		let code = vDesc.getCode();
		code = ShaderCache_default.substitute(code, "//VTK::Position::Impl", [
			"output.tcoordVS = vec2<f32>(vertexBC.x * 0.5 + 0.5, 1.0 - vertexBC.y * 0.5 - 0.5);",
			"output.Position = vec4<f32>(vertexBC, 1.0);",
			"output.vertexVC = vec4<f32>(vertexBC, 1);"
		]).result;
		vDesc.setCode(code);
	};
	model.shaderReplacements.set("replaceShaderPosition", publicAPI.replaceShaderPosition);
	publicAPI.updateBuffers = () => {
		const buff = model.device.getBufferManager().getFullScreenQuadBuffer();
		model.vertexInput.addBuffer(buff, ["vertexBC"]);
		model.numberOfVertices = 6;
	};
}
var DEFAULT_VALUES = {};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	SimpleMapper_default.extend(publicAPI, model, initialValues);
	vtkWebGPUFullScreenQuad(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkWebGPUFullScreenQuad");
var FullScreenQuad_default = {
	newInstance,
	extend
};
//#endregion
export { FullScreenQuad_default as default, extend, newInstance };

//# sourceMappingURL=FullScreenQuad.js.map