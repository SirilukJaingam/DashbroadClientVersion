import macros_default from "../../macros.js";
import { radiansFromDegrees } from "../Core/Math.js";
import ImplicitFunction_default from "./ImplicitFunction.js";
//#region Sources/Common/DataModel/Cone/index.js
function vtkCone(publicAPI, model) {
	model.classHierarchy.push("vtkCone");
	publicAPI.evaluateFunction = (x) => {
		const tanTheta = Math.tan(radiansFromDegrees(model.angle));
		return x[1] * x[1] + x[2] * x[2] - x[0] * x[0] * tanTheta * tanTheta;
	};
	publicAPI.evaluateGradient = (x) => {
		const tanTheta = Math.tan(radiansFromDegrees(model.angle));
		return [
			-2 * x[0] * tanTheta * tanTheta,
			2 * x[1],
			2 * x[2]
		];
	};
}
var DEFAULT_VALUES = { angle: 15 };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	ImplicitFunction_default.extend(publicAPI, model, initialValues);
	macros_default.setGet(publicAPI, model, ["angle"]);
	vtkCone(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkCone");
var Cone_default = {
	newInstance,
	extend
};
//#endregion
export { Cone_default as default, extend, newInstance };

//# sourceMappingURL=Cone.js.map