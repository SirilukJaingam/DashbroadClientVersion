import macros_default from "../../macros.js";
import Polygon_default from "../../Common/DataModel/Polygon.js";
import PolyData_default from "../../Common/DataModel/PolyData.js";
//#region Sources/Filters/General/TriangleFilter/index.js
var { vtkWarningMacro } = macros_default;
function vtkTriangleFilter(publicAPI, model) {
	model.classHierarchy.push("vtkTriangleFilter");
	publicAPI.requestData = (inData, outData) => {
		const input = inData[0];
		const points = input.getPoints().getData();
		const polys = input.getPolys().getData();
		const cellsDataType = input.getPolys().getDataType();
		const pointsDataType = input.getPoints().getDataType();
		const newCells = [];
		const newPoints = [];
		model.errorCount = 0;
		if (polys) {
			let npts = 0;
			let isLastPointDuplicated = false;
			for (let c = 0; c < polys.length; c += npts + 1) {
				npts = polys[c];
				isLastPointDuplicated = polys[c + 1] === polys[c + npts];
				if (isLastPointDuplicated) --npts;
				const cellPoints = [];
				cellPoints.length = npts;
				for (let i = 0; i < npts; i++) {
					const pointId = polys[c + i + 1];
					cellPoints[i] = [
						points[3 * pointId],
						points[3 * pointId + 1],
						points[3 * pointId + 2]
					];
				}
				if (npts === 3) {
					const newIdStart = newPoints.length / 3;
					newCells.push(3, newIdStart, newIdStart + 1, newIdStart + 2);
					newPoints.push(...cellPoints[0], ...cellPoints[1], ...cellPoints[2]);
				} else if (npts > 3) {
					const polygon = Polygon_default.newInstance();
					polygon.setPoints(cellPoints);
					if (!polygon.triangulate()) {
						vtkWarningMacro(`Triangulation failed at cellOffset ${c}`);
						++model.errorCount;
					}
					const newCellPoints = polygon.getPointArray();
					const numSimplices = Math.floor(newCellPoints.length / 9);
					const triPts = [];
					triPts.length = 9;
					for (let i = 0; i < numSimplices; i++) {
						for (let j = 0; j < 9; j++) triPts[j] = newCellPoints[9 * i + j];
						const newIdStart = newPoints.length / 3;
						newCells.push(3, newIdStart, newIdStart + 1, newIdStart + 2);
						newPoints.push(...triPts);
					}
				}
				if (isLastPointDuplicated) ++npts;
			}
		}
		const dataset = outData[0]?.initialize() || PolyData_default.newInstance();
		dataset.getPoints().setData(macros_default.newTypedArrayFrom(pointsDataType, newPoints));
		dataset.getPolys().setData(macros_default.newTypedArrayFrom(cellsDataType, newCells));
		outData[0] = dataset;
	};
}
var DEFAULT_VALUES = { errorCount: 0 };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.setGet(publicAPI, model, []);
	macros_default.get(publicAPI, model, ["errorCount"]);
	macros_default.obj(publicAPI, model);
	macros_default.algo(publicAPI, model, 1, 1);
	vtkTriangleFilter(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkTriangleFilter");
var TriangleFilter_default = {
	newInstance,
	extend
};
//#endregion
export { TriangleFilter_default as default, extend, newInstance };

//# sourceMappingURL=TriangleFilter.js.map