import macros_default from "../../../macros.js";
//#region Sources/Widgets/Core/StateBuilder/cornerMixin.js
function vtkCornerMixin(publicAPI, model) {
	publicAPI.translate = (dx, dy, dz) => {
		const [x, y, z] = publicAPI.getCornerByReference();
		publicAPI.setCorner(x + dx, y + dy, z + dz);
	};
}
var DEFAULT_VALUES = { corner: [
	0,
	0,
	0
] };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.setGetArray(publicAPI, model, ["corner"], 3);
	vtkCornerMixin(publicAPI, model);
}
var cornerMixin_default = { extend };
//#endregion
export { cornerMixin_default as default, extend };

//# sourceMappingURL=cornerMixin.js.map