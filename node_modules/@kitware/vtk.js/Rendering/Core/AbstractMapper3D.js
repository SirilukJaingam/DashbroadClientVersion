import macros_default from "../../macros.js";
import BoundingBox_default from "../../Common/DataModel/BoundingBox.js";
import AbstractMapper_default from "./AbstractMapper.js";
//#region Sources/Rendering/Core/AbstractMapper3D/index.js
function vtkAbstractMapper3D(publicAPI, model) {
	publicAPI.computeBounds = () => {
		macros_default.vtkErrorMacro(`vtkAbstractMapper3D.computeBounds - NOT IMPLEMENTED`);
	};
	const superGetBounds = publicAPI.getBounds;
	publicAPI.getBounds = () => {
		publicAPI.computeBounds();
		return superGetBounds();
	};
	const superGetBoundsByReference = publicAPI.getBoundsByReference;
	publicAPI.getBoundsByReference = () => {
		publicAPI.computeBounds();
		return superGetBoundsByReference();
	};
	publicAPI.getCenter = () => {
		const bounds = publicAPI.getBoundsByReference();
		model.center = BoundingBox_default.isValid(bounds) ? BoundingBox_default.getCenter(bounds) : null;
		return model.center?.slice();
	};
	publicAPI.getLength = () => {
		const bounds = publicAPI.getBoundsByReference();
		return BoundingBox_default.getDiagonalLength(bounds);
	};
}
var defaultValues = (initialValues) => ({
	bounds: [...BoundingBox_default.INIT_BOUNDS],
	center: [
		0,
		0,
		0
	],
	viewSpecificProperties: {},
	...initialValues
});
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, defaultValues(initialValues));
	AbstractMapper_default.extend(publicAPI, model, initialValues);
	macros_default.setGet(publicAPI, model, ["viewSpecificProperties"]);
	macros_default.getArray(publicAPI, model, ["bounds"], 6);
	vtkAbstractMapper3D(publicAPI, model);
}
var AbstractMapper3D_default = { extend };
//#endregion
export { AbstractMapper3D_default as default, extend };

//# sourceMappingURL=AbstractMapper3D.js.map