import macros_default from "../../macros.js";
import CompositeCameraManipulator_default from "./CompositeCameraManipulator.js";
import CompositeMouseManipulator_default from "./CompositeMouseManipulator.js";
import MouseCameraTrackballRotateManipulator_default from "./MouseCameraTrackballRotateManipulator.js";
import MouseCameraTrackballRollManipulator_default from "./MouseCameraTrackballRollManipulator.js";
//#region Sources/Interaction/Manipulators/MouseCameraTrackballMultiRotateManipulator/index.js
function max(x, y) {
	return x < y ? y : x;
}
function sqr(x) {
	return x * x;
}
function vtkMouseCameraTrackballMultiRotateManipulator(publicAPI, model) {
	model.classHierarchy.push("vtkMouseCameraTrackballMultiRotateManipulator");
	const rotateManipulator = MouseCameraTrackballRotateManipulator_default.newInstance();
	const rollManipulator = MouseCameraTrackballRollManipulator_default.newInstance();
	let currentManipulator = null;
	publicAPI.onButtonDown = (interactor, renderer, position) => {
		const viewSize = interactor.getView().getViewportSize(renderer);
		const viewCenter = [.5 * viewSize[0], .5 * viewSize[1]];
		const rotateRadius = .9 * max(viewCenter[0], viewCenter[1]);
		const dist2 = sqr(viewCenter[0] - position.x) + sqr(viewCenter[1] - position.y);
		if (rotateRadius * rotateRadius > dist2) currentManipulator = rotateManipulator;
		else currentManipulator = rollManipulator;
		currentManipulator.setButton(publicAPI.getButton());
		currentManipulator.setShift(publicAPI.getShift());
		currentManipulator.setControl(publicAPI.getControl());
		currentManipulator.setCenter(publicAPI.getCenter());
		currentManipulator.onButtonDown(interactor, position);
	};
	publicAPI.onButtonUp = (interactor) => {
		if (currentManipulator) currentManipulator.onButtonUp(interactor);
	};
	publicAPI.onMouseMove = (interactor, renderer, position) => {
		if (currentManipulator) currentManipulator.onMouseMove(interactor, renderer, position);
	};
}
var DEFAULT_VALUES = {};
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	CompositeMouseManipulator_default.extend(publicAPI, model, initialValues);
	CompositeCameraManipulator_default.extend(publicAPI, model, initialValues);
	vtkMouseCameraTrackballMultiRotateManipulator(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkMouseCameraTrackballMultiRotateManipulator");
var MouseCameraTrackballMultiRotateManipulator_default = {
	newInstance,
	extend
};
//#endregion
export { MouseCameraTrackballMultiRotateManipulator_default as default, extend, newInstance };

//# sourceMappingURL=MouseCameraTrackballMultiRotateManipulator.js.map