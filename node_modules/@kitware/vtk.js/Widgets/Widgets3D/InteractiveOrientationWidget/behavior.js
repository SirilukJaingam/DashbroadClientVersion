import macros_default from "../../../macros.js";
//#region Sources/Widgets/Widgets3D/InteractiveOrientationWidget/behavior.js
function widgetBehavior(publicAPI, model) {
	model.classHierarchy.push("vtkInteractiveOrientationWidgetProp");
	macros_default.event(publicAPI, model, "OrientationChange");
	publicAPI.handleRightButtonPress = (e) => {
		if (!model.activeState || !model.activeState.getActive() || !model.pickable) return macros_default.VOID;
		publicAPI.invokeOrientationChange({
			action: "rightPress",
			event: e,
			...model.activeState.get("up", "right", "direction")
		});
		return macros_default.EVENT_ABORT;
	};
	publicAPI.handleLeftButtonPress = (e) => {
		if (!model.activeState || !model.activeState.getActive() || !model.pickable) return macros_default.VOID;
		publicAPI.invokeOrientationChange({
			action: "leftPress",
			event: e,
			...model.activeState.get("up", "right", "direction")
		});
		return macros_default.EVENT_ABORT;
	};
}
//#endregion
export { widgetBehavior as default };

//# sourceMappingURL=behavior.js.map