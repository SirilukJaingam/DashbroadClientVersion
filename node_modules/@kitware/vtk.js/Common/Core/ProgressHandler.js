import macros_default from "../../macros.js";
//#region Sources/Common/Core/ProgressHandler/index.js
function vtkProgressHandler(publicAPI, model) {
	model.classHierarchy.push("vtkProgressHandler");
	publicAPI.startWork = () => {
		model.workCount += 1;
		if (model.workCount === 1) publicAPI.invokeChange(true);
	};
	publicAPI.stopWork = () => {
		model.workCount -= 1;
		if (model.workCount === 0) publicAPI.invokeChange(false);
	};
	publicAPI.isWorking = () => !!model.workCount;
	publicAPI.wrapPromise = (promise) => {
		publicAPI.startWork();
		return new Promise((resolve, reject) => {
			promise.then((...resolveArgs) => {
				publicAPI.stopWork();
				resolve(...resolveArgs);
			}, (rejectError) => {
				publicAPI.stopWork();
				reject(rejectError);
			});
		});
	};
	publicAPI.wrapPromiseFunction = (fn) => (...args) => publicAPI.wrapPromise(fn(...args));
}
var DEFAULT_VALUES = { workCount: 0 };
function extend(publicAPI, model, initialValues = {}) {
	Object.assign(model, DEFAULT_VALUES, initialValues);
	macros_default.obj(publicAPI, model);
	macros_default.event(publicAPI, model, "change");
	macros_default.get(publicAPI, model, ["workCount"]);
	vtkProgressHandler(publicAPI, model);
}
var newInstance = macros_default.newInstance(extend, "vtkProgressHandler");
var ProgressHandler_default = {
	newInstance,
	extend
};
//#endregion
export { ProgressHandler_default as default, extend, newInstance };

//# sourceMappingURL=ProgressHandler.js.map