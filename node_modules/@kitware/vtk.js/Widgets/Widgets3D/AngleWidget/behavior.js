import macros_default from "../../../macros.js";
import { add } from "../../../Common/Core/Math.js";
import BoundingBox_default from "../../../Common/DataModel/BoundingBox.js";
import PointPicker_default from "../../../Rendering/Core/PointPicker.js";
//#region Sources/Widgets/Widgets3D/AngleWidget/behavior.js
var MAX_POINTS = 3;
function widgetBehavior(publicAPI, model) {
	model.classHierarchy.push("vtkAngleWidgetProp");
	model._isDragging = false;
	const picker = PointPicker_default.newInstance();
	picker.setPickFromList(1);
	publicAPI.getBounds = () => model.widgetState.getHandleList().reduce((bounds, handle) => BoundingBox_default.inflate(BoundingBox_default.addPoint(bounds, ...handle.getOrigin()), publicAPI.getScaleInPixels() ? 0 : handle.getScale1() / 2), [...BoundingBox_default.INIT_BOUNDS]);
	publicAPI.setDisplayCallback = (callback) => model.representations[0].setDisplayCallback(callback);
	function ignoreKey(e) {
		return e.altKey || e.controlKey || e.shiftKey;
	}
	publicAPI.handleLeftButtonPress = (e) => {
		if (!model.activeState || !model.activeState.getActive() || !model.pickable || ignoreKey(e)) return macros_default.VOID;
		picker.initializePickList();
		picker.setPickList(publicAPI.getNestedProps());
		const manipulator = model.activeState?.getManipulator?.() ?? model.manipulator;
		const { worldCoords } = manipulator.handleEvent(e, model._apiSpecificRenderWindow);
		if (model.activeState === model.widgetState.getMoveHandle() && model.widgetState.getHandleList().length < MAX_POINTS && manipulator) {
			const moveHandle = model.widgetState.getMoveHandle();
			moveHandle.setOrigin(...worldCoords);
			const newHandle = model.widgetState.addHandle();
			newHandle.setOrigin(...moveHandle.getOrigin());
			newHandle.setColor(moveHandle.getColor());
			newHandle.setScale1(moveHandle.getScale1());
			newHandle.setManipulator(manipulator);
		} else if (model.dragable) {
			model._isDragging = true;
			model._apiSpecificRenderWindow.setCursor("grabbing");
			model._interactor.requestAnimation(publicAPI);
		}
		publicAPI.invokeStartInteractionEvent();
		return macros_default.EVENT_ABORT;
	};
	publicAPI.handleMouseMove = (callData) => {
		const manipulator = model.activeState?.getManipulator?.() ?? model.manipulator;
		if (manipulator && model.pickable && model.dragable && model.activeState && model.activeState.getActive() && !ignoreKey(callData)) {
			const { worldCoords, worldDelta } = manipulator.handleEvent(callData, model._apiSpecificRenderWindow);
			if ((model.activeState === model.widgetState.getMoveHandle() || model._isDragging) && worldCoords.length && model.activeState.setOrigin) {
				const curOrigin = model.activeState.getOrigin();
				if (curOrigin) model.activeState.setOrigin(add(curOrigin, worldDelta, []));
				else model.activeState.setOrigin(worldCoords);
				publicAPI.invokeInteractionEvent();
				return macros_default.EVENT_ABORT;
			}
		}
		if (model.hasFocus) model._widgetManager.disablePicking();
		return macros_default.VOID;
	};
	publicAPI.handleLeftButtonRelease = () => {
		if (!model.activeState || !model.activeState.getActive() || !model.pickable) return macros_default.VOID;
		if (model.hasFocus && model.widgetState.getHandleList().length === MAX_POINTS) {
			publicAPI.loseFocus();
			return macros_default.VOID;
		}
		if (model._isDragging) {
			model._apiSpecificRenderWindow.setCursor("pointer");
			model.widgetState.deactivate();
			model._interactor.cancelAnimation(publicAPI);
			model._isDragging = false;
		} else if (model.activeState !== model.widgetState.getMoveHandle()) model.widgetState.deactivate();
		if (model.hasFocus && !model.activeState || model.activeState && !model.activeState.getActive()) {
			model._widgetManager.enablePicking();
			model._interactor.render();
		}
		publicAPI.invokeEndInteractionEvent();
		return macros_default.EVENT_ABORT;
	};
	publicAPI.grabFocus = () => {
		if (!model.hasFocus && model.widgetState.getHandleList().length < MAX_POINTS) {
			model.activeState = model.widgetState.getMoveHandle();
			model.activeState.activate();
			model.activeState.setVisible(true);
			model._interactor.requestAnimation(publicAPI);
			publicAPI.invokeStartInteractionEvent();
		}
		model.hasFocus = true;
	};
	publicAPI.loseFocus = () => {
		if (model.hasFocus) {
			model._interactor.cancelAnimation(publicAPI);
			publicAPI.invokeEndInteractionEvent();
		}
		model.widgetState.deactivate();
		model.widgetState.getMoveHandle().deactivate();
		model.widgetState.getMoveHandle().setVisible(false);
		model.widgetState.getMoveHandle().setOrigin(null);
		model.activeState = null;
		model.hasFocus = false;
		model._widgetManager.enablePicking();
		model._interactor.render();
	};
}
//#endregion
export { widgetBehavior as default };

//# sourceMappingURL=behavior.js.map